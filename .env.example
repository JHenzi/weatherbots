# =============================================================================
# weather-trader — example environment variables
# Copy this file to .env and fill in real values. Never commit .env or secrets.
# =============================================================================

# -----------------------------------------------------------------------------
# Timezone (optional)
# Cron and dashboard use this for local time. Default: America/New_York
# -----------------------------------------------------------------------------
# TZ=America/New_York

# -----------------------------------------------------------------------------
# Weather / forecast API keys (optional for basic run; needed for full forecasts)
# -----------------------------------------------------------------------------
# Visual Crossing — used for historical + forecast; required for some data paths
VISUAL_CROSSING_API_KEY=your_visual_crossing_key_here

# Tomorrow.io (optional)
TOMORROW=your_tomorrow_io_api_key_here

# WeatherAPI.com (optional)
WEATHERAPI=your_weatherapi_com_key_here

# Google Weather (optional; GOOGLE or GOOGLE_WEATHER_API_KEY)
GOOGLE=your_google_weather_key_here

# OpenWeatherMap (optional). Set DISABLE_OPENWEATHERMAP=1 to skip.
OPENWEATHERMAP_API_KEY=your_openweathermap_api_key_here

# Pirate Weather (optional)
PIRATE_WEATHER_API_KEY=your_pirate_weather_api_key_here

# Required for api.weather.gov (NWS) requests. Include contact info per NWS guidance.
NWS_USER_AGENT=weather-trader (contact: you@example.com)

# -----------------------------------------------------------------------------
# Kalshi Trade API v2 (required for trading and dashboard Kalshi features)
# -----------------------------------------------------------------------------
# Environment: demo or prod
KALSHI_ENV=demo

# Your Kalshi API Key ID (NOT the private key itself)
KALSHI_API_KEY_ID=your_kalshi_key_id_here

# Path to your RSA private key PEM file.
# In Docker, use a path inside the container (e.g. /run/secrets/kalshi_key.pem).
KALSHI_PRIVATE_KEY_PATH=/absolute/path/to/your-kalshi-key.pem

# Optional: override API base URL (usually not needed)
# KALSHI_BASE_URL=

# Optional: override high-temp series tickers per city (defaults: KXHIGHNY, KXHIGHCHI, KXHIGHAUS, KXHIGHMIA)
# KALSHI_SERIES_NY=KXHIGHNY
# KALSHI_SERIES_IL=KXHIGHCHI
# KALSHI_SERIES_TX=KXHIGHAUS
# KALSHI_SERIES_FL=KXHIGHMIA

# -----------------------------------------------------------------------------
# Postgres (optional — mirror/read from DB; see README “Data persistence”)
# -----------------------------------------------------------------------------
# Connection URL. PGDATABASE_URL or DATABASE_URL
# PGDATABASE_URL=postgresql://user:password@host:5432/weather_trader

# Enable writing to Postgres (mirror of CSV). Set to 1/true/yes/y to enable.
# ENABLE_PG_WRITE=0

# Enable reading from Postgres with CSV fallback. Set to true/1/yes/y to enable.
# ENABLE_PG_READ=false
